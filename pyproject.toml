[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[project]
name = "ufo"
version = "0.1.0"
description = "Unreliable but Fast Optics code"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.maturin]
features = ["python"]
